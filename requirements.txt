# Core LlamaIndex and LLM dependencies
llama-index
llama-index-embeddings-openai
llama-index-llms-openai
llama-index-llms-openai-like
llama-index-llms-perplexity
llama-index-vector-stores-qdrant

# Data ingestion and processing
datasets
huggingface-hub
# Structure-aware attachment chunking (#89): pandas splits spreadsheets by
# row-groups so large sheets aren't truncated at bge-m3's 8192-token limit.
pandas

# Vector storage and utilities
python-dotenv
pydantic
qdrant-client
FlagEmbedding

# Optional: For better CLI and logging
tqdm
questionary
# Full-screen TUI for `mailrag wizard` (src/tui)
textual

# MCP server (src/mcp_server): expose the query path over Model Context Protocol
mcp

# Continuous sync (src/sync, issue #101): PyYAML reads accounts.yaml; imapclient
# is the IMAP leg (ENABLE / CONDSTORE / CHANGEDSINCE, which imaplib lacks).
# imapclient is only needed for the IMAP source — Maildir sync works without it.
PyYAML
imapclient

# pytest for testing
pytest